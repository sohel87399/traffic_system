services:
  - type: web
    name: traffic-system
    env: python3
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run Scripts/traffic_dashboard.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true